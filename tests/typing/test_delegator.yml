- case: delegator
  main: |
    from atom.api import Atom, Delegator, Int

    class A(Atom):

      i = Int(strict=False)
      m = Delegator(i)

    reveal_type(A.m)  # N: Revealed type is "atom.delegator.Delegator[builtins.int, builtins.float]"
    reveal_type(A().m)  # N: Revealed type is "builtins.int*"
